/** @deprecated Superseded by `engine.ts` (theme-driven). Kept as a re-export
 *  so older imports keep working. */
export { buildCountdown } from "./engine";
export type { EngineRefs } from "./engine";
